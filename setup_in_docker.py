# Placeholder for setup_in_docker.py
# This script is called during Docker build
# Originally used for Cython compilation, but skipped for local dev builds
print("Setup in Docker: Skipping compilation for local dev build")
